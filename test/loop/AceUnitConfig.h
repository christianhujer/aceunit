/** Use return to exit failed test cases. */
#define ACEUNIT_ASSERTION_STYLE ACEUNIT_ASSERTION_STYLE_RETURN

/** Let AceUnit use its own C99 definitions. */
#define ACEUNIT_INTERNAL_ISO_TYPES

/** This test uses the loop feature. */
#define ACEUNIT_LOOP
